---
title: Elsewhere
format: html
test:
  formats: [html]
---

The project writes to `out` rather than `_output`. The render layer reads the
directory Quarto resolved, or it reports a working render as writing nothing.

{{< present >}}
